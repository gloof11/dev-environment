# dev-environment
My development environment inside of a docker container to make it portable across any environment.

# Usage
You can pull the environment from docker hub with

```docker pull gloof11/dev-environment```

You can use the ```run.sh``` to quickly spin it up. Or add your own volumes.
